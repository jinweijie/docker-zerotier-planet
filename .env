# ================================
# Container identity & image
# ================================
CONTAINER_NAME=zerotier_planet
# Your script chooses image based on whether a Docker proxy is set.
# Compose can't auto-detect that, so set it explicitly if needed.
# If you have a working proxy in /etc/docker/daemon.json, you can keep the official:
ZT_IMAGE=jinweijiedocker/zerotier-planet:latest
# If you want to pull from a third-party mirror, set:
# ZT_IMAGE=jinweijiedocker/zerotier-planet:latest

# ================================
# Network configuration
# Match your script’s prompts & envs
# ================================
# Public IPv4 (required)
IP_ADDR4=<IP_ADDR4>
# Public IPv6 (optional; leave empty if not used)
IP_ADDR6=

# Ports (host:container are the same numbers, like in the script)
# Example defaults shown in your script prompts
ZT_PORT=9994
API_PORT=3443
FILE_PORT=3000

# ================================
# Host bind mount paths
# These mirror your script’s directories under ./data/zerotier
# ================================
DIST_PATH=./data/zerotier/dist
ZTNCUI_PATH=./data/zerotier/ztncui
ONE_PATH=./data/zerotier/one
CONFIG_PATH=./data/zerotier/config